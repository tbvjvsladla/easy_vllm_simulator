# testlog 260607-6 — 서브노드 싱글노드 빌드+서빙 교차검증

## 목적
통신·코드이식·에이전트통신이 갖춰진 뒤, **서브노드(spark-bdc9)가 메인과 동일하게 싱글노드(0.18.0 kanana)
컨테이너를 독립 빌드하고 서빙하는지** 확인(빌드 교차검증 + 서브 환경 동형성 입증).

## 절차 / 결과 (전부 통과)
1. **pre-flight (no-download)**: 서브 `/mnt/llm` NAS autofs 마운트 확인, kanana 모델 실재(12 items),
   docker-compose `/app/models:ro` 매핑 동일. ✅
2. **코드 이식**: `sync_to_sub.sh --apply` (메인 main브랜치 0.18.0 워킹트리 → 서브).
   체크섬 일치(Dockerfile/docker-compose/requirements), CLAUDE.md(서브 페르소나) 제외·보존. ✅
3. **빌드**: 서브 `docker compose --profile debug build` → exit 0,
   `vllm_serving_server-vllm:latest` (sha256 b7b472e1, 21.5GB). NGC 26.01 base ~9GB pull 포함.
   pip resolver 경고는 비치명적(메인과 동일). ✅ **빌드 교차검증 성립**.
4. **서빙**: 서브 `docker compose --profile serve up` → "Application startup complete" (~200s, 모델 로드 포함). ✅
5. **추론 스모크**: 서브 엔드포인트(:8920) → kanana 추론 →
   `{"content":"<SAFE>"}` → **PASS** (메인과 동일 응답). ✅
6. **복원**: 테스트용 단일노드 덮어쓰기를 `git checkout -- . && git clean -fd -e CLAUDE.md` 로
   multi-node(0.21.0)로 복원, CLAUDE.md 보존, git 정상. ✅

## 결론
서브노드가 메인과 **완전히 동일하게** 빌드·서빙(kanana→`<SAFE>`). 서브 빌드 환경 동형성 + sync 이식 +
빌드워커 셋업이 모두 실증. 멀티노드 확장의 토대(네트워크·SSH·코드이식·CC통신·빌드교차검증)가 전부 검증됨.

## 비고 (다음 멀티노드 실검증 시)
- 서브 첫 빌드 ~9GB pull로 장시간 → 직접 SSH 백그라운드로 안정 실행(claude -p Bash 타임아웃 회피). CC통신은 별도 검증(devlog_260607_5 §4.5).
- 서브 claude 호출은 `ssh sub 'bash -lc "claude -p ..."'` (login shell PATH).
- 멀티노드 실검증은 대형 MoE(예 Qwen3.5-397B-A17B-NVFP4) NAS 사전배치 + master(메인)+slave(서브) Ray 클러스터 동시 기동 필요.
