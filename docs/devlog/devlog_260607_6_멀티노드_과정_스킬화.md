# devlog 260607-6 — 멀티노드 과정을 스킬에 녹임 (문서화 + 헬퍼 스크립트)

## 작업
end-to-end 검증한 멀티노드 셋업·서빙 과정(testlog_260607_7)을 `upstream-version-watch` 스킬의
영속 자산으로 인코딩. 범위: **문서화 + 헬퍼 스크립트**(사용자 선택). 기존 sync_to_sub.sh 재사용.

## 산출물
1. **scripts/multinode_serve_smoke.sh** (신규, 검증됨): 멀티노드 2노드 Ray 서빙+multi-smoke 결정론 오케스트레이션.
   `<config_name> [--build] [--keep-up]` → NAS체크 → (양노드 병렬빌드) → master+slave Ray클러스터 →
   **엔드포인트 health 폴링**(거짓양성 회피) → master 추론(reasoning 대비 max_tokens) → 정리.
   검증: gpt-oss-120b-MXFP4 실행 → READY ~280s → SMOKE PASS content='4' fr=stop → exit 0.
2. **SKILL.md §4.5 멀티노드 경로**: 절차(resolve→patch→sync→multi-smoke) + 학습 7종 + §6 스크립트 목록.
3. **workflow.md**: S2(멀티노드 보존+gitignore 정렬) · **S2.5 sync**(sync_to_sub.sh 직접 전달) ·
   S3(multinode_serve_smoke.sh + 엔드포인트 health 판정 + OOM/NCCL→Model-C) · S4(서브 전파=rsync 직접).

## 인코딩된 핵심 학습
- 빌딩블럭 gitignored → 브랜치 전환 persist, cross-branch 동기화 불필요(단 multi-node .gitignore 정렬).
- 준비판정 = 엔드포인트 :PORT/health http200 (master 로그 "startup complete"는 거짓양성 → grep 금지).
- reasoning 모델(gpt-oss) → max_tokens 충분히(finish_reason=stop).
- 서브 빌드 = 직접 SSH 백그라운드(긴 빌드는 claude -p 타임아웃). 서브 CC = `ssh sub 'bash -lc "claude -p"'`.
- master만 API → 프로브 master 엔드포인트만. 멀티노드 실패(OOM/NCCL/Ray)=Model-C.
- 빌드 교차검증(서브 독립 빌드 동형성).

## 상태
멀티노드 과정이 스킬에 인코딩됨. 단일노드(Phase 1)+멀티노드 양쪽이 스킬-driven으로 재현 가능.
미커밋: main 0.18.0 패치 + multi-node 0.18.0 패치(스모크 통과, 커밋/푸시는 사람 승인 시).
