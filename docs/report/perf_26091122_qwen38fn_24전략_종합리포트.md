# Qwen3.8-Flash-Next × vLLM 0.29.0rc6 · multi TP=2(GB10×2) — 24전략 종합 리포트

> **SUPERSEDED-IN-PART** (2026-09-13) — 후속 캠페인 `camp-26091216-qwen38fn-res-min` 이 이 문서의
> **§4 판정(“res 축은 12/12 실패로 취급하는 것이 안전하다”)을 뒤집었다.** PLE resident 는 불가능이
> 아니었고, context 를 한 토큰도 낮추지 않은 채 서빙된다. §6 의 태그 표도 2건이 재발행·은퇴로
> 바뀌었다. 후속: `docs/report/perf_26091305_qwen38fn_res최소조건_후속캠페인.md`
> (이 문서의 §1~§3·§5·§7 관측은 그대로 유효하다.)

> 범위: 이 모델의 **유일한 검증 토폴로지**(multi TP=2, GB10×2 클러스터)에서 정의된 전체 전략
> 매트릭스 24종 = 체크포인트 변종(2: NVFP4/FP8) × KV dtype(2: auto/fp8_e4m3) × context(3: 262k/512k/1m)
> × PLE 적재모드(2: mmap/resident). 단일노드 트랙은 이 모델에 대해 존재하지 않는다(원격 hint 태그
> 조회로 확인, `gb10x2-cluster-native` 계열만 존재).
> 근거 문서 체인: `docs/plan/plan_26091108_qwen38fn_22셀_재수행_캠페인.md` ·
> `docs/report/perf_26091114_qwen38fn_22셀재수행_7셀체크포인트.md` ·
> `docs/report/perf_26091117_qwen38fn_22셀재수행_512k그룹완료체크포인트.md` ·
> `docs/report/perf_26091121_qwen38fn_22셀재수행_캠페인완주.md`(직전 체크포인트, 22/22 완주)
> 이 문서는 그 22셀에 **레거시 2셀**(직전 `camp-26090918` 24셀 캠페인에서 hint 발행이 이미 끝나
> 재수행 대상에서 제외된 셀)을 더해 **24전략 전체를 한 표로** 엮는다.

## 1. 24전략 전체 매트릭스

| 셀 | 변종 | kv dtype | context | PLE | 사인 | decode t/s@conc1 | 증거등급 |
|---|---|---|---|---|---|---|---|
| `nv4-bf-262k-mmp` | NVFP4 | auto | 262144 | mmap | **measured** | **35.39** | ✅ 인증서 발행(2026-09-13 재수행 · 38.81 t/s) |
| `nv4-bf-262k-res` | NVFP4 | auto | 262144 | resident | **measured** | **32.53** | ✅ 후속 재확립(2026-09-13 · 46.70 t/s · §4 정정) |
| `nv4-f8-262k-mmp` | NVFP4 | fp8_e4m3 | 262144 | mmap | **measured** | **38.18** | ✅ 인증서 발행 |
| `nv4-f8-262k-res` | NVFP4 | fp8_e4m3 | 262144 | resident | serve_failed | — | watchdog_kill_ack |
| `fp8-bf-262k-mmp` | FP8 | auto | 262144 | mmap | **measured** | **35.37** | ✅ 인증서 발행 |
| `fp8-bf-262k-res` | FP8 | auto | 262144 | resident | void | — | budget_block |
| `fp8-f8-262k-mmp` | FP8 | fp8_e4m3 | 262144 | mmap | **measured** | **34.04** | ✅ 인증서 발행 |
| `fp8-f8-262k-res` | FP8 | fp8_e4m3 | 262144 | resident | void | — | budget_block |
| `nv4-bf-512k-mmp` | NVFP4 | auto | 524288 | mmap | **measured** | **33.72** | ✅ 인증서 발행 |
| `nv4-bf-512k-res` | NVFP4 | auto | 524288 | resident | serve_failed | — | watchdog_kill_ack |
| `nv4-f8-512k-mmp` | NVFP4 | fp8_e4m3 | 524288 | mmap | **measured** | **34.86** | ✅ 인증서 발행 |
| `nv4-f8-512k-res` | NVFP4 | fp8_e4m3 | 524288 | resident | serve_failed | — | watchdog_kill_ack |
| `fp8-bf-512k-mmp` | FP8 | auto | 524288 | mmap | **measured** | **37.61** | ✅ 인증서 발행 |
| `fp8-bf-512k-res` | FP8 | auto | 524288 | resident | void | — | budget_block |
| `fp8-f8-512k-mmp` | FP8 | fp8_e4m3 | 524288 | mmap | **measured** | **35.94** | ✅ 인증서 발행 |
| `fp8-f8-512k-res` | FP8 | fp8_e4m3 | 524288 | resident | void | — | budget_block |
| `nv4-bf-1m-mmp` | NVFP4 | auto | 1048576 | mmap | **measured** | **35.92** | ✅ 인증서 발행 |
| `nv4-bf-1m-res` | NVFP4 | auto | 1048576 | resident | serve_failed | — | watchdog_kill_ack |
| `nv4-f8-1m-mmp` | NVFP4 | fp8_e4m3 | 1048576 | mmap | **measured** | **33.40** | ✅ 인증서 발행 |
| `nv4-f8-1m-res` | NVFP4 | fp8_e4m3 | 1048576 | resident | serve_failed | — | watchdog_kill_ack |
| `fp8-bf-1m-mmp` | FP8 | auto | 1048576 | mmap | **measured** | **35.39** | ✅ 인증서 발행 |
| `fp8-bf-1m-res` | FP8 | auto | 1048576 | resident | void | — | budget_block |
| `fp8-f8-1m-mmp` | FP8 | fp8_e4m3 | 1048576 | mmap | **measured** | **33.27** | ✅ 인증서 발행 |
| `fp8-f8-1m-res` | FP8 | fp8_e4m3 | 1048576 | resident | void | — | budget_block |

**종결 24/24(100%) · 측정 13/24(54%)** — mmp 계열 12/12 전부 성공, res 계열 12/12 전부 실패
(watchdog_kill_ack 5건 · budget_block 6건 · **단 1건 예외**, §4 참조).

## 2. 핵심 축 — PLE mmap vs resident가 서빙 생사를 가른다

컨텍스트 길이(262k/512k/1m) · 체크포인트 변종(NVFP4/FP8) · KV dtype(auto/fp8) 어느 조합에서도
PLE mmap 축을 벗어난 예외 없이 재현됐다(§4의 레거시 1건 제외).

- **mmp(PLE mmap) 계열 12/12 성공** — 262k 4종 + 512k 4종 + 1m 4종 전부 PASS. t/s 범위 33.27~38.18.
- **res(PLE resident) 계열은 체크포인트 크기에 따라 실패 기전이 갈린다**:
  - **NVFP4(123.57GiB, 작은 체크포인트)** — 예산 선판정은 항상 통과한다(floor=16,064MiB, 양수·context
    무관 고정값). 그러나 실제 로드 중(`determine_available_memory` RPC 단계) 실측 상주메모리가 예측을
    초과해 워치독에 사살된다. 재현: 262k 1건(`nv4-f8-262k-res`) + 512k 2건 + 1m 2건 = **5/5**.
  - **FP8(172.78GiB, 큰 체크포인트)** — 예산 선판정 단계에서 즉시 음수 floor(−9,134MiB)로 차단된다
    (로드 0초, GPU 비용 0). 재현: 262k 2건 + 512k 2건 + 1m 2건 = **6/6**.
  - KV 절대클램프(20,480MiB 고정)는 **context 길이 무관**임이 3개 그룹에서 반복 확인됐다 — 동일 변종의
    floor 값이 262k·512k·1m 전부 정확히 일치한다.

> **정정(2026-09-13 · `camp-26091216`)** — 위 관측은 정확하나 **원인 귀속이 틀렸다.** 예산 공식
> `floor = MemTotal − weights − kv − overhead` 는 맞고 라이브 4회 1MiB 오차 없이 재현되지만,
> res 를 막은 것은 그 예산이 아니라 **`gpu-memory-utilization` 천장**이었다. KV 를 20,480 → 8,192 로
> 회수하면 floor 는 예측대로 28,352 로 오르는데 **회수한 12,288MiB 를 로드 피크가 12,392 되먹는다**
> — vLLM 이 천장까지 쓰므로 KV 를 비우면 다른 버퍼가 들어온다. 천장을 0.85 → 0.80 으로 내리자
> 워치독 임계 대비 여유가 +390 → **+5,952MiB** 로 벌어지며 서빙이 성립했다.

## 3. 측정값 상세 — 동시성 벡터(mmp 12셀 전체)

| 셀 | conc=1 | conc=2 | conc=4 | conc=8 | conc=16 | verdict floor |
|---|---|---|---|---|---|---|
| nv4-bf-262k-mmp(레거시) | 35.39 | 29.53 | 22.64 | 15.90 | 11.51 | 2.57 |
| nv4-bf-262k-res(레거시) | 32.53 | 31.40 | 24.40 | 18.91 | 11.81 | 3.10 |
| nv4-f8-262k-mmp | 38.18 | 32.97 | 24.78 | 16.98 | 11.66 | 2.57 |
| fp8-bf-262k-mmp | 35.37 | 28.49 | 18.49 | 13.91 | 8.60 | 2.52 |
| fp8-f8-262k-mmp | 34.04 | 25.98 | 19.55 | 13.17 | 8.78 | 2.52 |
| nv4-bf-512k-mmp | 33.72 | 33.29 | 22.23 | 15.89 | 10.50 | 2.57 |
| nv4-f8-512k-mmp | 34.86 | 29.74 | 20.02 | 15.04 | 9.61 | 2.57 |
| fp8-bf-512k-mmp | 37.61 | 27.27 | 19.88 | 14.25 | 8.44 | 2.52 |
| fp8-f8-512k-mmp | 35.94 | 25.72 | 18.14 | 12.53 | 8.77 | 2.52 |
| nv4-bf-1m-mmp | 35.92 | 32.66 | 25.36 | 16.81 | 10.77 | 2.57 |
| nv4-f8-1m-mmp | 33.40 | 29.23 | 22.20 | 14.67 | 10.09 | 2.57 |
| fp8-bf-1m-mmp | 35.39 | 28.50 | 19.37 | 13.93 | 8.77 | 2.52 |
| fp8-f8-1m-mmp | 33.27 | 25.85 | 18.20 | 12.46 | 8.44 | 2.52 |

전부 `--authority explore`(서술, 성능 게이트 아님) · 완결 엔드포인트(`/v1/completions`) ·
`vllm bench serve` 표준 도구 · warmup 2 폐기 후 산출. 레거시 2셀은 인증서 없이 `hint_map_only`
등급으로 발행됐으므로 baseline로 승격하지 말라는 경고가 원 태그에 명시돼 있다(§4).

## 4. 정직성 노트 — nv4-bf-262k-res 레거시 "성공"은 재현 대상이 아니다

24전략 표에서 **유일하게 res 축이 성공한 셀**은 `nv4-bf-262k-res`(레거시, 32.53 t/s)다. 이것이
"resident 모드도 262k에서는 괜찮다"는 결론으로 읽혀서는 안 된다 — 이유는 다음과 같다:

1. **이 측정은 R0~R10 하네스 교정 이전**(`camp-26090918`, 2026-09-10 이전)의 환경에서 나왔다. 이번
   22셀 재수행 캠페인이 존재하는 이유 자체가 그 캠페인의 24셀 중 21셀이 사인 오진이었기 때문이다
   (`docs/testlog/testlog_26091109_하네스교정_R0R10_역채점_음성대조.md`). 워치독 임계값·예산 게이트
   구성이 현재와 동일했다는 보증이 없다.
2. **같은 res 축(NVFP4+resident)을 하네스 교정 후 재시험한 5건(`nv4-f8-262k-res` +
   `nv4-{bf,f8}-512k-res` + `nv4-{bf,f8}-1m-res`)은 전부 워치독 사살로 실패**했다 — 동일 floor
   (16,064MiB) 위에서 5전 5패다. `nv4-bf-262k-res`만 성공한 것은 재현되지 않는 예외로 다뤄야 한다.
3. 원 hint 태그 자신이 **"OBSERVATION-ONLY. 인증서가 없다 — baseline이나 권고로 승격하지 마라"**고
   명시하고 있다(§7 메타: 24셀 중 measured 3 / void 21).

**결론(발행 시점)**: 24전략 중 **실질적으로 신뢰 가능한 권고 축은 mmp 12/12**이며, res 축은
(레거시 1건의 불확실한 예외를 제외하면) **12/12 실패**로 취급하는 것이 안전하다.

> ### ⚠ 이 결론은 뒤집혔다 (2026-09-13 · `camp-26091216-qwen38fn-res-min`)
>
> **res 축은 실패하는 축이 아니라 예산이 잘못 배분된 축이었다.** 위 §4-2 가 "동일 floor 위에서
> 5전 5패"라고 적은 것은 사실이지만, 그 floor 를 만든 KV 20,480MiB 자체가 262k 이론 최소치
> (노드당 3,072MiB)의 6.7배 과잉이었고 진짜 제약은 §2 정정이 말한 gmu 천장이었다.
>
> 확정 조건 `ctx 262144(무손실) · KV 8,192MiB/노드 · max-num-seqs 8 · max-num-batched-tokens 2048
> · gpu-memory-utilization 0.80` 에서 **decode 46.70 t/s**(동시성1)로 서빙되며, 이는 같은 캠페인의
> mmap 자매셀(38.81)을 **전 동시성 구간에서 앞선다**. PLE 가 메모리에 있으면 n-gram 룩업이 매 토큰
> NVMe gather 를 타지 않기 때문이다. verdict `PASS`(authority explore · like-with-like 외부 문턱
> 53.7 · 달성률 0.87) · 인증서 발행 완료.
>
> 위 근거 ①②③ 중 **①(하네스 교정 전후 환경 차이)은 이번에 음성대조로 해소됐다** — mmp 재수행이
> 레거시와 같은 범위를 재현했다(38.81 vs 35.39 · 나머지 레벨 ±5%). ③(원 태그의 observation-only
> 경고)은 태그 자체가 은퇴하며 해소됐다(§6).

## 5. R8(YaRN 컨텍스트 확장) 검증 — 512k·1m 전 조합 완결

262k는 모델 네이티브 상한이라 YaRN이 불필요하다. 512k(factor=2)·1m(factor=4) 양쪽에서
`mrope_interleaved=true · partial_rotary_factor=0.25 · rope_theta=10000000 · rope_type=yarn ·
original_max_position_embeddings=262144` 오버라이드 위에 YaRN factor만 전환하는 방식이 NVFP4/FP8
변종, auto/fp8 KV dtype 전 조합(8셀)에서 정상 동작을 재현했다 — 완결 엔드포인트 추론 정합까지 확인.

## 6. hint 태그 카탈로그 — mmp 12전략 + res 1전략 (2026-09-13 갱신)

`hint/0.29.0/qwen3.8-flash-next-{nvfp4,fp8}/gb10x2-cluster-native/` 아래 **13건**이 발행돼 있다
(`HINTS.md` 참조) — mmp 12전략 + **res 1전략**(후속 캠페인이 연 축 · §4 정정).

| kv/ctx | NVFP4 | FP8 |
|---|---|---|
| 262k-auto | `len262144-kvauto-plemmap` | `len262144-kvauto-plemmap` |
| 262k-fp8e4m3 | `len262144-kvfp8e4m3-plemmap` | `len262144-kvfp8e4m3-plemmap` |
| 512k-auto | `len524288-kvauto-plemmap` | `len524288-kvauto-plemmap` |
| 512k-fp8e4m3 | `len524288-kvfp8e4m3-plemmap` | `len524288-kvfp8e4m3-plemmap` |
| 1m-auto | `len1048576-kvauto-plemmap` | `len1048576-kvauto-plemmap` |
| 1m-fp8e4m3 | `len1048576-kvfp8e4m3-plemmap` | `len1048576-kvfp8e4m3-plemmap` |

### res 축 (2026-09-13 신설)

| kv/ctx | NVFP4 |
|---|---|
| 262k-auto | `len262144-kvauto-pleresident` |

**✅ 해소됨** — 발행 시점의 `nv4-bf-262k-{mmp,res}` 2건은 R9(PLE축 태그명 교정) **이전** 발행분이라
`-plemmap` 접미사가 없었고(`len262144-kvauto` / `len262144-kvauto_260910t0204z`), 더 중요하게는
`task_class=hint_map_only` 로 올라가 **인증서가 없었다**(그 클래스의 required evidence 에 certificate
가 없다 — 이것이 결손의 진짜 원인이었다).

2026-09-13 후속 캠페인이 두 셀을 재수행해 `task_class=full_benchmark` 로 재발행했고, 옛 태그 2건은
**로컬·원격에서 은퇴**시켰다(원격 잔존 0 · 카탈로그는 원격 발행 태그에서 재파생). 두 새 태그는
모두 verdict PASS 인증서를 갖는다.

## 7. 알려진 배선 결함(누적, 최종)

- 서브측 워치독은 두 개의 독립 인스턴스(ad-hoc non-journald / systemd L1 유닛 `easy-vllm-memwatch`)로
  나뉘며, 262k~1m 전 구간에서 둘 다 최소 1회씩 실제 사살을 집행했다. journald 경유 유닛은
  `blackbox_events.py`를 서브에서 직접 실행해 자동 수확 가능하다.
- 메인 노드 `--keep-up` 워치독·예산갱신 루프의 과거 `&`(비-setsid) 결함은 이번 22셀 전 구간에서
  재발하지 않았다(Monitor 기반 실행으로 전환한 이후 안정).
- 브랜치 전환(single-node → multi-node, 512k 그룹 도중 사용자 지시로 수행)은 데이터 손실 없이
  완료됐다 — `campaigns/`·`output/multi/{configs,envs}`는 gitignored라 브랜치 전환에 영향받지 않는다.

## 8. 다음 단계(후속 과제, 이번 리포트 범위 밖)

- [x] `nv4-bf-262k-mmp` 레거시 셀을 R9 태그명 규약으로 재검증·재발행(§6) — **2026-09-13 완료**
      (`len262144-kvauto-plemmap` · 인증서 발행 · 38.81 t/s).
- [x] purge 게이트 선행조건 확인 후 다음 캠페인 착수 — **2026-09-12 완료**. 다만 게이트가 P2·P3 에서
      막혔다: 이 캠페인은 완주하고 증거도 22건 실재했으나 `phases/` 진행표가 비어 있었다(phase writer
      미호출 = 침묵 누락). 실제 증거에 묶어 진행표를 채운 뒤 열었다.
- [ ] `sync_branches.sh`의 전체동기화 설계와 노드별 특화의 긴장 — 사용자가 명시적으로 후속과제로 미룸
      (2026-09-11, 브랜치 미스매치 적발 시점).
- [ ] 후속 캠페인이 남긴 6건은 `docs/report/perf_26091305_qwen38fn_res최소조건_후속캠페인.md` §7 참조
      (KV dtype fp8 · context 확장 · `max-num-seqs` 상향 · FP8 res 에 gmu 레버 · 예산 선언의 gmu 입력화 ·
      `judge_bench` accept_len 승계).
