# plan 260607-6 — 멀티노드 2노드 Ray 서빙 검증 (gpt-oss-120b-MXFP4 / 0.18.0)

> 양 노드 multi-node 브랜치 0.18.0 셋업 완료 후, 실제 2노드 Ray 클러스터 서빙 + multi-smoke 검증.
> 전제(충족): 양 노드 0.18.0, gpt-oss-120b 모델 NAS 실재, serve_runner.sh, CC통신. 근거: Seed seed_8f97998bdec0.

## 대상
- 모델: gpt-oss-120b-MXFP4 (config.yaml smoke.multi_node). 양 노드 NAS 실재 확인됨.
- 토폴로지: master=메인(192.168.100.10, Ray head + vllm serve), slave=서브(192.168.100.11, Ray worker).

## 절차
1. **빌드** (양 노드, 최소병렬): 메인 + 서브 각각 멀티노드 0.18.0 이미지 빌드(네트워크 apt 추가분 재빌드).
   - 메인: 직접 build / 서브: background 병렬. NGC 26.01 베이스 캐시됨 → apt+wheel+requirements 위주.
2. **Ray 클러스터 기동**:
   - 메인: `docker compose --env-file envs/.env.gpt-oss-120b-MXFP4 --profile master up` → Ray head + slave 합류 대기(5분) → gpt-oss-120b serve.
   - 서브: `--profile slave up` → master Ray head 합류(worker).
3. **준비 신호**: 메인 "Application startup complete"(2-GPU Ray 클러스터 형성 후).
4. **multi-smoke**: master 엔드포인트(90XX)로 gpt-oss-120b 추론 1회 → 비어있지 않은 완성.
5. **정리**: 양 노드 down.

## 실패 분기 (failure_patterns / Model-C)
- OOM / NCCL-RDMA / Ray join timeout → 서브 build/serve 실패는 멀티노드 서빙 불가 → Model-C(HITL) 보고.
- 빌드 누락 라이브러리 → requirements-fixable.

## 완료 기준
- 양 노드 빌드 exit 0, Ray 2-GPU 클러스터 형성, master 엔드포인트 추론 비어있지 않은 응답.
- 실패 시 분류 + 보고(소스빌드/조정 판단). 검증 로그 docs/testlog.
