# plan 260607-3 — 스킬 정밀화: 결정론 보강 + 확률론 안정화 계획서

> 싱글노드 `upstream-version-watch` 스킬(Phase 1 검증 완료)을 시드 `seed_801f0bf97156` 명세대로
> 다듬는다. 이 문서는 *계획서* — 실제 구현 전 검토용. 승인 후 착수.
> 근거: 인터뷰 `interview_20260607_075652` → Seed `seed/seed_801f0bf97156.yaml` (QA 0.91 PASS).

## 0. 목표 / 경계
- **목표**: (A) 결정론 로직 보강 + (B) 확률론 로직 안정화. 스크립트=사실, LLM=판단(하네스 엔지니어링).
- **스코프**: 고정 4단계 패턴(wheel fetch → NGC선정+deps조정 → 기동+실패분류 → 스모크+확정).
- **경계**: 소스빌드는 '필요함을 감지'까지만(범위 밖=Phase 2). 멀티노드 범위 밖. 모델 다운로드 금지.

## 1. Track A — 결정론 보강

| # | 작업 | 산출물 | 근거(시드 AC) |
|---|---|---|---|
| A1 | `regen_requirements.py`를 **wheel METADATA Requires-Dist 모드**로 리팩터 (extra 포함, pip transitive) | scripts/regen_requirements.py | uvloop 누락 해결(이번 세션 검증) |
| A2 | **no-download NAS 체크 스크립트**: `configs/<config_name>.yaml`의 model 경로를 읽어 `/app/models` 마운트 하 실재 확인(없으면 비0+보고) | scripts/check_smoke_model.py | NAS 체크 스크립트화 |
| A3 | **failure_patterns.yaml** + 분류 로직: signature-regex → class(requirements-fixable / source-build-class). 시드 엔트리 시딩 | failure_patterns.yaml + 분류 helper | 결정론 분류기 |

- A3 시드 엔트리(이 세션 로그):
  - `ModuleNotFoundError: <lib>` → requirements-fixable
  - `libcudart\.so\.\d+: cannot open` → requirements-fixable
  - `undefined symbol` / `register_opaque_type.*hoist` / `No module named 'torch\._opaque_base'` → source-build-class

## 2. Track B — 확률론 안정화

| # | 작업 | 산출물 | 근거(시드 AC) |
|---|---|---|---|
| B1 | **사실-테이블 렌더러**: change-summary / layer-bump-proposal의 사실 행(torch핀·NGC태그·wheel URL·deps delta·ARG)을 resolve 스크립트 JSON에서 그대로 렌더(LLM 사실 작성 0) | 렌더 helper + SKILL.md 규약 | 사실 byte-match |
| B2 | **risk-memo 규약**: LLM은 리스크 해석 + step-3 분류(근거 포함)만. 사실 미도입 | SKILL.md §확률론 | risk-memo만 확률론 |
| B3 | **Model-C HITL 게이트**: 미지 실패 → LLM {proposed_class, evidence} 제시 → 사람 승인 전 무행동. 신규 패턴은 agent diff 제안→승인 후 codify | SKILL.md/workflow.md | Model-C |
| B4 | **Loop-Until-Done + cap**: 조정가능 실패는 (조정→재빌드→스모크) 성공까지, config cap(기본 3) 한정. cap 소진→Model-C 결정 | workflow.md + config.yaml(cap 필드) | Loop-Until-Done |

## 3. 문서화 (Phase-1 마감)
- `SKILL.md`/`.claude/rules/workflow.md`/`config.example.yaml` 갱신(위 규약 반영, cap 필드 추가).
- `docs/devlog`에 Phase-1 마감 기록(정밀화 요약 + 0.18.0 앵커 검증 + 교훈).

## 4. 완료 기준 (시드 AC 발췌)
- [ ] regen=METADATA → 빌드 후 `pip list`에 uvloop 존재(수동추가 없이)
- [ ] NAS 체크 스크립트: 존재 exit0 / 부재 비0+보고(다운로드 0)
- [ ] failure_patterns.yaml 분류 + 미지→Model-C 게이트
- [ ] 사실 행 = 스크립트 JSON byte-match(LLM 사실 0), risk-memo만 LLM
- [ ] Loop-Until-Done cap(기본3, config) + cap소진→Model-C
- [ ] 소스빌드급 감지·정지(propose Y/N) — 0.22.1/26.03 재현
- [ ] 0.18.0 앵커 E2E 재현(서빙+추론) — 이미 검증, 회귀 확인
- [ ] 문서화 완료

## 5. 실행 순서 (제안)
A1(검증됨, 최저위험) → A2 → A3 → B1 → B4(cap) → B3 → B2 → 문서화. 각 산출 md/스크립트는
사용자 검토 게이트 후 다음으로. (A1은 이미 이번 세션에서 동작 확인 → 코드화만.)

## 6. 확인 요청
1. 실행 순서(A1부터) 동의?
2. failure_patterns.yaml 위치: `.claude/skills/upstream-version-watch/failure_patterns.yaml`(스킬 자기완결) 동의?
3. cap 기본값 3 / config.yaml 필드명 `reconciliation_cap` 동의?
