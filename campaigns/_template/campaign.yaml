{
  "_howto": [
    "캠페인 선언(뼈대). 확장자는 .yaml 이지만 내용은 순수 JSON 텍스트다 — 파서는 stdlib json 하나이며 PyYAML 은 이 저장소의 의존성이 아니다(registry.yaml 선례).",
    "채우는 순서: 사용자가 빈칸(<<FILL>>)을 채우고, 에이전트가 파생값을 채운다. <<FILL>> 이 하나라도 남으면 검증기가 fail-closed 한다.",
    "검증: python3 .claude/skills/terraforming_node/scripts/campaign_template_validator.py --campaign campaigns/<camp-id>/campaign.yaml",
    "이 선언이 producer 경로의 원천이다 — recipe --config · broad_search --state · relay 원장 루트가 모두 여기서 파생된다(workflow.md §producer 경로 파생)."
  ],
  "schema_version": 1,

  "id": "<<FILL>>",
  "plan_ref": "<<FILL>>",
  "declared_utc": "<<FILL>>",

  "nodes": [
    {"node_id": "<<FILL>>", "role": "main", "topology": "<<FILL>>", "hw": "<<FILL>>"}
  ],

  "matrix": {
    "versions": ["<<FILL>>"],
    "models": ["<<FILL>>"]
  },

  "order": ["<<FILL>>"],

  "budgets": {
    "smoke_budget_overhead_mib": 0,
    "ready_max_seconds": 0
  },

  "control_variables": {
    "model": "<<FILL>>",
    "vllm_version": "<<FILL>>",
    "topology": "<<FILL>>",
    "target_gpu": "<<FILL>>"
  },

  "_revisions_howto": [
    "layer-1(통제변인) 개정 이력. revisions[0] 은 t0 원본이며 **불변**이고, 위 control_variables 와 같아야 한다.",
    "개정은 append-only 이고 **메인 단일 창구**다 — 서브는 릴레이로 요청만 하고 자기 선언에는 메인 선언을 그대로 echo 한다.",
    "기입 경로: campaign_init.py --revise --revise-set K=V --revise-reason ... --revise-evidence ... --revise-approved-by ... --utc ...",
    "각 항목은 사유·근거 경로·승인자·시각 4필드가 필수다. 넷 중 하나라도 없으면 그것은 개정이 아니라 드리프트다.",
    "layer-2 셀 축(kv dtype·attention·MoE·batch)은 여기 오지 않는다 — 그쪽은 셀의 axis_citation 이 자율로 든다."
  ],
  "revisions": [],

  "hint_targets": [
    {"arch": "<<FILL>>", "node_id": "<<FILL>>", "cells": []}
  ],

  "topology_sections": {
    "single": {},
    "multi": {}
  }
}
