# Warm-start

> Durable per-thread context packets are written here on (re)index
> (`warm-start/<thread>.md` = the highest-authority entries for an active thread).
> Create extra packets only when an interview/query benefits from one.
