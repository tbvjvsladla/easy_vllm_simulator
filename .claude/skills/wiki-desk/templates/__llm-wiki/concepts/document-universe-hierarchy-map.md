---
title: document-universe-hierarchy-map
type: concept
confidence: high
---

# Document Universe Hierarchy Map

> Authority order + query routing. (Regenerated by init_wiki_desk.py from config.)

## Authority order

- (filled at init from authority_order — e.g. devlog:100 > testlog:85 > sub-doc:70 > plan:55 > simlog:40 > seed:25)

## Query routing

- completion ("did it work"): testlog, simlog, devlog
- intent ("what approach"): devlog, plan, sub-doc, seed
- Resolve via metadata/edges first; open a source body only when the summary is insufficient.
- Negative honesty: if nothing clears the authority/score gate, return "no evidence" — never fabricate a path.

## Constitution is NOT indexed

The project ROOT constitution (`/CLAUDE.md` / `/.claude/`) is deliberately excluded (by-root;
vendored subtrees under `seed/` too) so the library stays a neutral evidence base that can
surface patterns not yet codified (anti-confirmation-bias). The constitution is the
human-authored *output* of reviewing the logs, not an input.
