# 3. 벤치 결과 — 그리고 무엇과 비교할 수 있나

> **인증서가 없다.** 이 절은 인증서 없이 발행된다 — 그것이 정책이다(강행 발행).
> 인증서는 full 모드 verdict==PASS 일 때만 나오며, 그 발행은 `adversarial-benchmark` 의
> 책임이다. 부재는 '성능이 나빴다' 가 아니라 '**그 형태로 판정되지 않았다**' 는 뜻이다.

> ⚠ 아래 정보가 **부재한 채로 발행**됐다. 부재는 실패가 아니라 **기록**이며, 이 태그를
> 소비할 때 그만큼을 모른 채 소비한다는 뜻이다(강행 발행 정책 · plan_26090616 Q7/Q8).

| 사유코드 | 뜻 |
|---|---|
| `HINT_MISSING_CERTIFICATE` | 인증서 부재 — full PASS 가 아니었거나 벤치마커가 발행하지 않았다. 인증서 발행은 adversarial-benchmark 의 책임이지 발행기의 책임이 아니다. |

## 부하 스윕 곡선 — 동시성별 (결정론 파싱 · 손저작 ✗)

> 단일 running serve 에 **동시 요청 수만** 바꿔 잰 곡선이다(reload 없음 · request-rate=inf).
> `동시성=1` 행이 인증서의 판정점이며, 나머지 행은 그 레시피가 **부하에서 어떻게 되는지**를 말한다.
> 출처: `bench_report_26091321_qwen3.8-flash-next-fp8_RTXPRO6000BlackwellMaxQWorkstationEdition_0.1.1.md` (bench_report) · 소수 2자리 표시 반올림 · 결측은 `N/A`(0 이 아니다).
> 이 표는 스크립트가 파싱해 렌더한다 — 손으로 옮긴 수치가 아니며 `--verify` 가 diff 0 을 요구한다.

| 동시성 | decode t/s | 출력 tok/s | 총 tok/s | TTFT p50(ms) | ITL p50(ms) | 완료/실패 |
|---|---|---|---|---|---|---|
| 1 ★판정점 | 12.40 | 12.27 | 61.40 | 113.43 | 80.55 | 32/0 |
| 2 | 11.97 | 23.88 | 119.48 | 267.62 | 82.86 | 32/0 |
| 4 | 11.88 | 47.55 | 237.92 | 370.99 | 83.05 | 32/0 |
| 8 | 5.96 | 47.63 | 238.32 | 21783.46 | 83.39 | 36/0 |

_절삭된 레벨 없음(요청 전 레벨 완주)._


## like-with-like 한정자 (Agent)

**인증서 없음 · verdict=REFUTE · authority=explore.** `explore` 권한은 **문턱을 세우지 않는다** —
성능·밸런스 축 모두 서술이고, 승격 자격은 성능 판정이 아니라 **서빙 성립 + 유효 측정**이다.
따라서 이 수치를 baseline 이나 권고로 읽지 마라. 루프라인 기대 achievable 19.49 t/s 대비
단일스트림 ratio **0.636**(floor 16.57) — 즉 MTP off 기준으로 메모리대역 상한의 그만큼만 쓴다.

**말할 수 있는 것**: 이 형상이 **뜬다**는 것, 그리고 이 측정조건에서의 부하 곡선 모양.
**말할 수 없는 것**: 다른 입력/출력 길이, 다른 배치 폭, MTP on 조건, 반복 측정 없이는 **5% 이내의
셀 간 차이**. 이 라운드는 셀당 1회 측정이다.

**like-with-like 한정자** — 아래가 하나라도 다르면 비교 불가:
입력 1024 · 출력 256 · 레벨당 32요청 · warmup 4 · `--backend openai`(완결 엔드포인트) ·
**`max-num-seqs: 4`** · KV 절대클램프 **4 GiB/GPU** · `enforce-eager` · TP=2 ·
GPU **Max-Q 판**(Server Edition 아님) · MTP **off** · 도구 GuideLLM(호스트 venv) · 절삭 0건.

특히 **MTP on/off** 를 밝히지 않은 수치와 비교하지 마라 — 공개 기준선 다수가 MTP 를 포함하고,
그걸 모르면 2배 차이를 하드웨어 차이로 오독한다(이 모델의 MTP k=3 은 별도 측정에서 2.13~2.33배).

⚠ **이 태그의 수치는 다른 셀과 동시성 8 에서 비교할 수 없다.** 이 형상만 `max-num-seqs: 4` 다 — VRAM 98.8% 점유라 레시피 생성 단계에서 배치 폭이 깎였다. 엔진이 `Running: 4 reqs, Waiting: 4` 를 찍고 KV 는 8.7% 만 썼다(캐시 부족 ✗ · 슬롯 부족 ○). **FP8 + PLE 상주는 262K 가 상한이다** — 512K·1M 은 로드 자체가 안 된다.
