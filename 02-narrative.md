# 2. 서사 — 빌드와 서빙이 어떻게 진행되었나

> 이 절은 **전부 Agent 저작**이다. 기계는 여기 한 글자도 쓰지 않는다(plan §6).
> **원문 전재 금지**(plan Q2) — devlog 실물에는 운영자 경로·호스트 지문이 있어
> 그대로 옮기면 배포 PII 표면이 커진다. **재현에 필요한 교훈만 추려 새로 쓴다.**

## 읽을 원재료 (복사 대상 아님 · 포인터)

- devlog: `../devlog/devlog_26091009_qwen38fn_24셀_캠페인종합_서사.md`
- testlog: `../testlog/testlog_26091009_qwen38fn_24셀_판정.md`

## 서사

**증상.** 24셀 매트릭스를 선언해 돌렸는데 **3셀만 측정되고 21셀이 void** 로 끝났다
(../testlog/testlog_26091009_qwen38fn_24셀_판정.md §판정). 실패가 한 원인이 아니라 세 갈래였고, 갈래마다 대응이 다르다.

**원인 ① — 선언 자체가 모델 상한을 넘었다(8셀).** `max_model_len` 을 524288·1048576 으로 잡은 셀들이
`ValidationError` 로 즉사했다. 모델의 `max_position_embeddings` 가 **262144** 이기 때문이다
(../testlog/testlog_26091009_qwen38fn_24셀_판정.md §void 셀 · NVFP4 512k/1m). 이건 튜닝으로 넘을 벽이 아니라 **선언이 틀린 것**이며, YaRN 같은
컨텍스트 확장을 적용하지 않는 한 그 셀들은 영원히 뜨지 않는다.

**원인 ② — FP8 가중치 변종이 일관되게 hang 했다(12셀).** `fp8-*` 계열 전부가 컨테이너 hang 후 자동
종료됐고, 컨테이너가 사라진 뒤라 로그가 부족해 정확한 단계는 **2차 추정**으로 남았다 —
`fused_moe` FP8 config 부재 경고가 종료 직전 로그에 있었다(../testlog/testlog_26091009_qwen38fn_24셀_판정.md §2 확정/추론 사실). 이 태그는
그 추정을 확정처럼 말하지 않는다.

**원인 ③ — 측정 도구가 측정 대상과 메모리를 다퉜다(1셀).** `nv4-f8-262k-mmp` 은 서빙에 성공했는데
sweep 벤치가 5레벨 전부 시스템 OOM kill 을 맞았다(3회 시도 동일). 벤치 이미지가 서빙 컨테이너 +
KV 풀과 함께 통합메모리 한계를 넘겼기 때문이다(../testlog/testlog_26091009_qwen38fn_24셀_판정.md §셀 4 의 측정 결손).

**해소.** 262144 이하 · NVFP4 가중치 조합의 셋만 살아남았고, 그 셋은 동일 빌드·동일 컨테이너
profile(eager · MTP k=3 · async off)에서 측정됐다(../testlog/testlog_26091009_qwen38fn_24셀_판정.md §2 확정 사실). 자체 이식 3종이 stock 0.29.0rc6
위에서 성립함이 이 측정으로 실증됐다 — 포크 핀은 쓰지 않았다.

## 되풀이하지 말 것

- **`max_model_len` 을 희망으로 잡지 마라.** 모델 config 의 `max_position_embeddings` 를 먼저 읽어라.
  이 캠페인은 그 하나를 건너뛰어 **24셀 중 8셀을 선언 단계에서 잃었다**. 확장이 필요하면 YaRN 을
  먼저 적용하고 그 사실을 provenance 로 남겨야 한다.
- **FP8 가중치 변종을 같은 매트릭스에 섞지 마라.** 12셀이 같은 원인으로 무너졌는데, 그 원인이
  `fused_moe` config 부재인지 KV 풀 양자화 단계인지 **아직 확정되지 않았다**. 확정 전에 셀을 늘리면
  실패 12개를 다시 사는 것이다. 먼저 1셀로 원인을 가르고 나서 확장하라.
- **벤치 도구를 서빙과 같은 호스트에 무겁게 얹지 마라.** 서빙이 성공했는데 측정이 파괴되면
  그 셀은 아무 정보도 남기지 못한다. 통합메모리 플랫폼에서는 무거운 벤치 컨테이너 대신
  `vllm bench serve` 처럼 가벼운 경로를 먼저 검토하라(../testlog/testlog_26091009_qwen38fn_24셀_판정.md §후속).
- **컨테이너가 hang 하면 죽기 전에 로그를 빼라.** 이 캠페인은 자동 종료 뒤에 원인을 물었고,
  그래서 12셀의 근본 원인이 **추론으로만** 남았다.
