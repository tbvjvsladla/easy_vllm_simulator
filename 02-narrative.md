# 2. 서사 — 빌드와 서빙이 어떻게 진행되었나

> 이 절은 **전부 Agent 저작**이다. 기계는 여기 한 글자도 쓰지 않는다(plan §6).
> **원문 전재 금지**(plan Q2) — devlog 실물에는 운영자 경로·호스트 지문이 있어
> 그대로 옮기면 배포 PII 표면이 커진다. **재현에 필요한 교훈만 추려 새로 쓴다.**

## 읽을 원재료 (복사 대상 아님 · 포인터)

- devlog: `../devlog/devlog_26090408_14_01_gptoss20b_gb10_0180_tp2.md`
- testlog: `../testlog/testlog_26090408_14_01_gptoss20b_gb10_0180_tp2.md`

## 서사

**이 항목은 같은 하드웨어에서 같은 엔진으로 먼저 뚫어 둔 120b 경로를 그대로 탔다.** 그래서 서사의
값어치는 "무엇이 새로 문제였나" 가 아니라 **"앞선 모델에서 배운 것 중 무엇이 이 모델에도 그대로
적용됐나"** 에 있다(testlog §5 · §3.1).

**한 번에 떴다.** 120b 에서 하나씩 벗겨 낸 층 — 이미지 트랙(멀티 Dockerfile) · `distributed-executor-backend`
· `gpu-memory-utilization` 0.80 · 호스트 네트워킹의 포트 의미 — 을 처음부터 반영한 트리플렛으로
시작했기 때문이다. **벽 지도의 값이 여기서 회수됐다**: 같은 벽을 두 번 맞지 않았다.

**단 하나 모델별로 다시 정해야 했던 것은 KV 다.** 워커당 5 GiB 로 두고 `max-model-len` 을 4096 으로
잡았다. 이 조합은 같은 모델의 단일노드 힌트(131072 · 총 10 GiB)와 **의도적으로 다르다** — 이번 목표는
최대 컨텍스트가 아니라 분산 경로의 성능이었다. KV 절대 클램프의 이식성은 이렇게 확인됐다: 총량을
같게 두면 TP=2 는 정확히 두 배의 토큰 용량을 낸다(총 10 GiB 기준 218,448 → 436,896 tokens · testlog §4).

**성능 관찰**(testlog §4): 단일노드 대비 1.43 배로, 같은 하드웨어의 120b(1.57 배)보다 이득이 **작다**.
원인은 이 모델이 단일노드에서 이미 대역폭을 86.6% 쓰고 있어 헤드룸이 적다는 것이다(120b 는 22% 여유).
그런데 분산 후 두 모델의 대역폭 활용률은 60.9% 와 61.9% 로 거의 같은 곳에 수렴한다 — **분산
오버헤드가 모델 크기보다 토폴로지에 더 의존한다**는 신호다.

## 되풀이하지 말 것

**"작은 모델이니 TP=2 이득이 클 것" 이라고 가정하지 마라.** 반대다. 단일노드에서 이미 대역폭을
거의 다 쓰고 있으면 노드를 늘려도 가져갈 헤드룸이 없다. 분산 이득을 예측하려면 **먼저 단일노드의
대역폭 활용률을 재라** — 그 값이 이득의 상한을 말한다.

**KV 클램프 숫자를 다른 모델에서 옮기지 마라.** 같은 계열의 더 큰 모델이 반드시 더 큰 KV 를 쓰는
것이 아니다 — 토큰당 KV 바이트는 레이어·헤드 구성과 `max-model-len` 에 함께 걸린다. 실측이 정본이다.

**★ 이 하드웨어에서 vLLM 0.19.1 은 분산 서빙이 안 된다.** 단일노드에서는 정상이므로 **단일만 재면
두 버전이 구분되지 않는다.** 분산에서는 `vllm/config/utils.py` 의 해시 팩터 수집에서
`_stateless_dp_group_port_list` 키가 처리되지 않아 컴파일이 실패한다. 이 모델과 같은 계열의 더 큰
모델에서 **정확히 같은 파일·함수·키**로 죽는 것을 확인했다 — 모델 특성이 아니라 단일 원인이다
(testlog §3.1). `enforce_eager` 우회도 실패한다: 초기화와 health 는 통과하지만 첫 추론에서 죽고,
그때의 오류 메시지는 2차 오류라 **진짜 원인을 가린다**. 멀티 계획이 있으면 버전을 여기서 고정하라.

**측정 방식을 바꾸지 마라.** 이 모델은 harmony 계열이라 chat 경로로 재면 `--ignore-eos` 가 무력화돼
TPOT 이 왜곡된다. 우리도 그 배선이 한 곳에서 빠져 값이 2.9 배 낮게 나온 적이 있고, 인증서에 실리기
직전에 잡았다.
