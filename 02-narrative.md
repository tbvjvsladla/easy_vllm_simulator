# 2. 서사 — 빌드와 서빙이 어떻게 진행되었나

> 이 절은 **전부 Agent 저작**이다. 기계는 여기 한 글자도 쓰지 않는다(plan §6).
> **원문 전재 금지**(plan Q2) — devlog 실물에는 운영자 경로·호스트 지문이 있어
> 그대로 옮기면 배포 PII 표면이 커진다. **재현에 필요한 교훈만 추려 새로 쓴다.**

## 읽을 원재료 (복사 대상 아님 · 포인터)

- devlog: `../devlog/devlog_26090408_37_20_gptoss120b_gb10_0180_single.md`
- testlog: `../testlog/testlog_26090408_37_20_gptoss120b_gb10_0180_single.md`

## 서사

**서빙 자체는 벽 없이 떴다.** 그래서 이 항목의 서사는 빌드·런타임이 아니라 **측정과 판정 평면**에
있다 — 그리고 거기서 두 개의 거짓이 나왔다(testlog §2·§3).

**① 측정 경로가 틀리면 수치가 3배 틀린다.** 이 모델은 harmony 계열이라 `/v1/chat/completions`
(하네스 기본 backend)로 재면 `--ignore-eos` 가 무력화된다. 같은 런에서 full 이 34.42 t/s 인데
lite 가 **11.83 t/s** 로 나왔다 — 2.9 배 차이다. 원인은 스윕 오케스트레이터가 레벨 호출에는
백엔드를 넘기면서 lite 호출에만 빼먹은 배선 누락이었고, **인증서에 실리기 직전에** 잡혔다.
값이 "좀 낮다" 가 아니라 **정의가 다른 것을 재고 있었다**.

**② 외부 레퍼런스가 우리 물리 상한을 넘었다.** 같은 모델·같은 GPU 의 공개 수치 57~60 t/s 를 찾았는데
(testlog §3), 그 구성은 dense 층까지 MXFP4 로 내리고 다른 attention/GEMM 백엔드를 쓴다. 우리 구성의
active 바이트는 dense 3.96 GiB(bf16) + routed 1.775 GiB(MXFP4) = 5.74 GiB 이고 대역폭 273 GB/s 로
나누면 **44.31 t/s 가 물리 상한**이다. 60 t/s 는 그 상한 위에 있으므로 우리 구성에서는 도달 불가능
하며, 문턱으로 쓰면 **부당한 기각**이 된다. 그래서 `e_search=empty` 로 기록하고 루프라인만으로
판정했다(음성 정직). **같은 모델·같은 하드웨어라도 양자화 범위와 커널 백엔드가 다르면 루프라인이
다르다 — like-with-like 의 축은 모델과 하드웨어만이 아니다.**

**③ 버전 선택에 대해 이 항목이 말할 수 있는 것.** 같은 하드웨어에서 0.19.1 은 34.55 t/s 로 이
버전(34.42)과 사실상 같고 스윕 곡선도 각 레벨 ±0.5 이내다. **단일노드만 보면 두 버전 사이에
고를 근거가 없다.** 근거는 분산에서 나온다(§되풀이하지 말 것).

## 되풀이하지 말 것

**측정 정의를 확인하지 않고 수치를 비교하지 마라.** 위 ①이 그 실증이다. gpt-oss 계열은
`/v1/completions` 경로로 재고, **총 생성 토큰이 요청값(프롬프트 수 × 출력 길이)과 정확히 일치하는지**
확인하라. 일치하지 않으면 EOS 로 조기 종료된 것이고 TPOT 이 의미를 잃는다.

**공개 수치를 문턱으로 쓰기 전에 물리 상한을 검산하라.** `active 바이트 ÷ 메모리 대역폭` 을 넘는
레퍼런스는 **우리 구성에서 도달 불가능**하다. 그런 값으로 기각하면 실제로는 정상인 서빙을 계속
재탐색하게 된다. 검산이 통과하지 못하면 그 레퍼런스는 like-with-like 가 아니다.

**★ 이 버전을 단일노드 성능만으로 고르지 마라.** 0.18.0 과 0.19.1 은 여기서 34.42 대 34.55 로
구분되지 않는다. 그런데 **분산에서는 0.19.1 이 아예 뜨지 못한다** —
`vllm/config/utils.py` 의 해시 팩터 수집에서 `_stateless_dp_group_port_list` 키가 처리되지 않아
컴파일이 실패하며, 이는 모델 무관이다(같은 계열 두 모델에서 같은 파일·함수·키). 지금은 단일노드만
쓰더라도 나중에 노드를 늘릴 계획이 있으면 그 사실이 선택을 바꾼다. 분산 재현 키트는 형제 태그
`hint/0.18.0/gpt-oss-120b/gb10-multi` 에 있다.

**KV 클램프 30 GiB 를 다른 토폴로지로 옮기지 마라.** 이 값은 **총량**이다. TP=2 설정의 같은 키는
**워커당** 값이라 그대로 넣으면 두 배를 잡는다.
