# 2. 서사 — 빌드와 서빙이 어떻게 진행되었나

> 이 절은 **전부 Agent 저작**이다. 기계는 여기 한 글자도 쓰지 않는다(plan §6).
> **원문 전재 금지**(plan Q2) — devlog 실물에는 운영자 경로·호스트 지문이 있어
> 그대로 옮기면 배포 PII 표면이 커진다. **재현에 필요한 교훈만 추려 새로 쓴다.**

## 읽을 원재료 (복사 대상 아님 · 포인터)

- devlog: `../devlog/devlog_26090706_캠페인7_GB10네이티브_재수행_서사.md`
- testlog: `../testlog/testlog_26090706_35_20_캠페인7_종결판정_커널축부재_2버전확증.md`

## 서사

**증상.** 이 판의 목표는 직전 캠페인의 **H100 이식 클램프를 걷어내고** GB10 자신의 예산으로 2노드
TP=2 를 세우는 것이었다. 네 개의 벽을 순서대로 만났고, 마지막 하나는 **해소가 없었다**.

**① 워치독 필터가 비었다.** 멀티 기동 시 `MASTER_CONTAINER_NAME 미설정` 으로 워치독의 감시 대상이
공집합이 됐다. 원인은 트리플렛 생성기가 단일형 `CONTAINER_NAME` 만 방출한 것 — 배선 부재이지 설정
실수가 아니다. 해소: 생성기를 `--topology multi` 로 불러 두 이름을 방출시킨다.

**② health 가 19분 침묵.** 서빙은 떴는데 폴링이 200 을 못 받았다. `network_mode: host` 라 config
yaml 의 포트가 env 를 **이긴다**. yaml 의 포트가 상수 8000 으로 하드코딩돼 있었고 env 는 다른 포트를
썼다. 손저작 시절에는 두 자리를 우연히 같게 적어 가려져 있었다 — 생성기로 옮기자 드러났다.
해소: yaml 포트를 env 에서 파생.

**③ overhead 이월이 거짓이었다.** KV 를 2.27배 키우자 실측 overhead 가 17,971 → 23,092 MiB 로
늘었다. 옛 값을 이월하면 5,121 MiB 과소가 되고, 그 차이만큼 로드 첨두에서 사살된다(CUDA 그래프
캡처 55/83 에서 TRIP). 해소: 실측 overhead 로 재산정 → KV 55,993 MiB/노드. 선언 floor 14,412 대
실제 바닥 14,149 로 오차 1.8%.

**④ SoC 열 hard ceiling — 해소가 없다(정상 차단).** 연속 포화부하 4분 22초에 SoC 가 78→95°C 로
올라 열 워치독이 `docker kill` 했다. 호스트 메모리도 GPU OOM 도 아니다. 근거: 판정 testlog
`testlog_26090706_캠페인7_C2_열벽_SoC_hard_ceiling_판정.md`. 하드웨어 `acpitz` critical 은 104°C 이고
부하 중 `SW/HW Thermal Slowdown` 증분은 **0 us** 였다 — 하드웨어는 스로틀하지 않았다. 같은 하드웨어의
single×20b 는 1.6시간 부하에 90°C 를 한 번 스칠 뿐이므로 **이 벽은 120b×multi 조합 고유**다.
NCCL 집단연산 조율이 GPU 가 아니라 SoC 를 데운다(부하 중 GPU 전력은 오히려 single 쪽이 높았다).

## 되풀이하지 말 것

- **열 임계를 올려서 넘어가려 하지 마라.** ④ 는 가드가 제 일을 한 **정상 차단**이다. 처방은 부하를
  열 예산 안으로 줄이는 것이지 임계 상향이 아니다. 이 조합의 상한은 동시성이 아니라 **시간**이다
  (연속 포화부하 4분 · SoC 가 93~94°C 에서 포화하고 ceiling 과 마진이 1~2°C).
- **overhead 를 다른 KV 값에서 이월하지 마라.** overhead 는 상수가 아니라 KV 의 함수다. 이 캠페인이
  그것을 5,121 MiB 오차로 실증했다.
- **attention·MoE 백엔드를 선언으로 바꾸려 하지 마라.** 0.19.0 은 대안 3종을 **명시적으로 거절**하고
  0.18.0 은 같은 선언을 **조용히 marlin 으로 폴백**시킨다. 버전에 따라 실패 방식이 다르므로 선언이
  아니라 **엔진 로그**를 봐라.
- **`fp8_e5m2` 를 KV dtype 으로 쓰지 마라.** 0.19.0 에서 `torch._dynamo.exc.Unsupported:
  Data-dependent assertion failed` 로 나온다. 실제 원인은 `attention.py:426` 의 assert 가
  `torch.compile` 안에 있는 것인데 **진단 메시지가 원인을 가린다** — 메시지만 보고 dynamo 를 의심하면
  하루를 잃는다.
- **KV 를 늘려 속도를 얻으려 하지 마라.** 2.27배 키워도 decode 는 +0.5% 다(50.58 → 50.85).
  KV 는 **용량 레버이지 속도 레버가 아니다.**
- **노드 간에 이미지를 옮기지 마라.** 양 노드가 같은 레시피로 **각자** 빌드한다. 노드마다 snapshotter
  가 달라 같은 내용이라도 크기가 다르며, 크기로 완료를 판정하면 위양성이 난다.
