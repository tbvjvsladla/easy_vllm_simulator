# 2. 서사 — 빌드와 서빙이 어떻게 진행되었나

> 이 절은 **전부 Agent 저작**이다. 기계는 여기 한 글자도 쓰지 않는다(plan §6).
> **원문 전재 금지**(plan Q2) — devlog 실물에는 운영자 경로·호스트 지문이 있어
> 그대로 옮기면 배포 PII 표면이 커진다. **재현에 필요한 교훈만 추려 새로 쓴다.**

## 읽을 원재료 (복사 대상 아님 · 포인터)

- devlog: `../devlog/devlog_26090706_캠페인7_GB10네이티브_재수행_서사.md`
- testlog: `../testlog/testlog_26090706_35_20_캠페인7_종결판정_커널축부재_2버전확증.md`

## 서사

**증상.** 이 캠페인의 목적은 "GB10 네이티브 예산"이었다 — 직전 캠페인이 외부 이식 타겟(H100)을
가정해 KV 를 산출했던 것과 달리, 이번에는 실행 하드웨어 자신의 통합메모리(121.69 GiB)를 예산의
근거로 삼는다. 첫 벽은 성능이 아니라 **호스트 안전**이었다: gmu 파생 KV 로 로드하면 워치독
트립선을 침범한다.

**원인.** 통합메모리 노드에서 `gpu_memory_utilization` 은 KV 를 정하는 노브가 아니다. 가중치·KV·
런타임 오버헤드가 **같은 풀**을 나눠 쓰므로 분율은 비결정론적으로 커지고, 남는 것(floor)이
워치독 임계 아래로 떨어진다. 근거: 셀 판정 testlog
`testlog_26090706_35_20_캠페인7_종결판정_커널축부재_2버전확증.md` §수렴 절과 서사 devlog
`devlog_26090706_캠페인7_GB10네이티브_재수행_서사.md`.

**해소.** KV 를 **절대 바이트로 클램프**하고 그 값을 실측에서 역산한다.
`required = per_token × max_model_len × batch` 로 필요량을 구하고,
`floor = mem_total − (weights + kv + overhead)` 가 트립선 위에 남는지 확인한다. 이 조합의 수렴값이
50,133 MiB(= 52,567,672,969 B) @ `max_model_len 131072` · `batch 16` 이고, overhead 실측은
12,265 MiB 였다. 두 번째 발견은 **커널 축이 비어 있었다**는 것이다 — 엔진 로그의
`Using ... out of potential backends: ['TRITON_ATTN']` 이 후보가 하나뿐임을 말하므로, attention 축을
"탐색"한 셀들은 사실 같은 백엔드를 반복해서 잰 것이었다. MoE 도 auto → MARLIN 하나로 접힌다
(같은 testlog 의 2버전 확증 절: 0.18.0 은 침묵 폴백, 0.19.0 은 명시 거절).

**세 번째로, 측정 유효성.** 이 판은 chat 엔드포인트로 쟀고 판정점 레벨에서 **16건 중 2건이
errored** 였다(위 §3 표의 완료/실패 열). gpt-oss 의 harmony 파서는 chat 스트림에서 깨질 수 있다.
같은 모델을 완결 엔드포인트로 잰 형제 태그(`gb10-sub-native`)는 18/18 · 오류 0 이다.
**엔드포인트가 다르면 두 수치는 like-with-like 가 아니다.**

## 되풀이하지 말 것

- **`gpu_memory_utilization` 으로 KV 를 정하려 하지 마라.** 통합메모리에서는 분율이 절대량을
  결정하지 못한다. 절대 바이트 클램프가 유일하게 결정론적인 노브다.
- **attention·MoE 백엔드를 "탐색"하기 전에 후보 목록부터 읽어라.** 엔진 로그가 후보를 print 한다.
  후보가 하나면 그 축은 실패가 아니라 **비어 있는 것**이고, 셀을 더 도는 것은 같은 값을 다시
  재는 일이다. 이 캠페인은 그것을 6셀 돌고 나서 알았다.
- **다른 노드의 overhead 를 이월하지 마라.** 12,265 MiB 는 이 조합(KV 50,133 · batch 16)의 실측이다.
  같은 모델을 batch 20 · KV 61,442 MiB 로 돌린 노드의 실측은 16,000 MiB 였다. 상수가 아니라
  KV·batch 의 함수이므로 **하한으로만** 써라.
- **chat 엔드포인트로 gpt-oss 를 재고 그 수치를 다른 판과 나란히 놓지 마라.** 파서 파손이 요청을
  errored 로 빼면 남은 표본이 편향된다. 완결 엔드포인트(`--backend openai`)가 이 모델군의 기본이다.
- **예산 선언 없이 로드하지 마라.** 선언이 없으면 워치독의 arm_ceiling 이 사실상 무한대가 되어
  방어가 꺼진다. 러너가 그 순서를 강제하는 이유다.
