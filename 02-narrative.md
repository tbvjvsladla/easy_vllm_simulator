# 2. 서사 — 빌드와 서빙이 어떻게 진행되었나

> 이 절은 **전부 Agent 저작**이다. 기계는 여기 한 글자도 쓰지 않는다(plan §6).
> **원문 전재 금지**(plan Q2) — devlog 실물에는 운영자 경로·호스트 지문이 있어
> 그대로 옮기면 배포 PII 표면이 커진다. **재현에 필요한 교훈만 추려 새로 쓴다.**

## 읽을 원재료 (복사 대상 아님 · 포인터)

- devlog: `../devlog/devlog_26091304_qwen38fn_res최소조건_후속캠페인.md`
- testlog: `../testlog/testlog_26091304_qwen38fn_res최소조건_262k재발행_판정.md`

## 서사

**증상** — 이 셀 자체는 벽이 아니다. PLE 를 NVMe mmap 으로 서빙하는 트랙은 직전 캠페인에서
12/12 성공했고 이번 재수행도 한 번에 섰다(testlog §1). 이 태그가 존재하는 이유는 **옛 발행분의
결손을 메우는 것**이다 — 종전 `len262144-kvauto`(무접미사) 태그는 R9 PLE축 명명 이전에 나갔고
**인증서가 없어 observation-only** 였다(직전 24전략 리포트 §4·§6).

**원인** — 옛 발행은 `task_class=hint_map_only` 로 올라갔다. 그 클래스의 required evidence 에는
인증서가 없다(`devlog·plan·testlog` 뿐). 클래스 선택이 곧 증거 등급을 정하는데, 그 사실이
발행 시점에 드러나지 않았다.

**해소** — 같은 축(ctx 262144 · KV auto 20,480MiB/노드 · PLE mmap · gmu 0.85 · MTP3)으로 재수행해
full 스윕을 돌리고 `task_class=full_benchmark` 로 발행했다. 그 클래스가 인증서를 required evidence
로 요구하므로 결손이 구조적으로 불가능하다.

**부수 소득 — 음성대조가 성립했다.** 이번 측정(38.81/31.10/23.05/16.73/10.93)이 R0~R10 하네스 교정
**이전**의 레거시 측정(35.39/29.53/22.64/15.90/11.51)과 같은 범위를 재현했다(testlog §1). 즉 그
교정은 측정 자체를 바꾸지 않았다 — 직전 캠페인의 사인 오진이 측정값까지 오염시켰는지가 열린
물음이었는데, 이 셀이 그 물음을 닫는다.

## 되풀이하지 말 것

- **mmap 트랙에서 gpu-memory-utilization 을 내리는 것** — 이 셀은 `0.85` 로 충분하다. PLE 가
  page cache 라 커널이 압박 때 evict 하므로 완충이 있다. 천장을 내리는 레버는 **resident 트랙의
  처방**이며(자매 태그 `…-pleresident` 참조), 여기 적용하면 KV 여유만 잃는다.
- **이 수치를 resident 자매셀과 같은 조건으로 보는 것** — 두 셀은 PLE 모드와 그에 딸린 네 설정이
  다르다. resident 쪽이 전 동시성 구간에서 앞선다(46.70 vs 38.81 @conc1).
- **옛 무접미사 태그(`len262144-kvauto`)를 baseline 으로 쓰는 것** — 그 태그 본문 자신이
  OBSERVATION-ONLY 를 명시했다. 이 태그가 그것을 대체한다.
