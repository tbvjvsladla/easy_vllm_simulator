# 2. 서사 — 빌드와 서빙이 어떻게 진행되었나

> 이 절은 **전부 Agent 저작**이다. 기계는 여기 한 글자도 쓰지 않는다(plan §6).
> **원문 전재 금지**(plan Q2) — devlog 실물에는 운영자 경로·호스트 지문이 있어
> 그대로 옮기면 배포 PII 표면이 커진다. **재현에 필요한 교훈만 추려 새로 쓴다.**

## 읽을 원재료 (복사 대상 아님 · 포인터)

- devlog: `../devlog/devlog_26090914_run_trial_native모드_추가_및_광의탐색_셀A.B.C_구성.md`
- testlog: `../testlog/testlog_26090915_Qwen3.8.27B_광의탐색_3셀_FullBench_비교.md`

## 서사

이 태그는 자매 hint `hint/0.28.0/qwen3.8-27b/rtxpro6000-main-native/qfp8-len262144-kvfp8`
(셀 B)와 **동일한 환경·동일한 벽**을 겪었다(같은 서빙 세션, 같은 광의탐색 캠페인의 다른 셀) —
Docker 부재·torchvision 누락·TP 오염·tool_call_parser 오독 방지는 그쪽 태그 본문(§1·§3)을
참조하라(중복 서술 대신 인용). 이 셀 고유의 차이점만 아래에 적는다.

**증상(이 셀 고유)**: 가중치를 양자화하지 않고(bf16 그대로) KV만 fp8 로 양자화하면, 동일
262144 컨텍스트에서 단일스트림 decode 속도가 fp8 가중치 자매셀(A: 43.91 t/s, B: 44.22 t/s)
대비 크게 낮다(이 셀: 26.83 t/s) — 결정근거는
`docs/testlog/testlog_26090915_Qwen3.8.27B_광의탐색_3셀_FullBench_비교.md` §결과 비교 표.
**원인**: bf16 가중치는 fp8 대비 토큰당 읽어야 할 바이트가 2배라 메모리대역폭 요구가 그만큼
크다 — 같은 이 셀 §외부 레퍼런스(b) 검증 이력이 밝히듯 `roofline.py` 는 이 가중치양자화
차이를 인지하지 못해(서빙시점 fp8/bf16 모두 같은 R_fp) fp8 케이스의 물리상한을 과소평가하는
결함이 있고, 그 결함을 피하려 **전 셀을 `--e-search empty`(외부 레퍼런스 미사용) + 결정론
루프라인(expected_achievable, TP=1 기준 11.29 t/s) 단독 판정으로 재통일**했다(같은 testlog
§외부 레퍼런스(b) 참조 — 더 보수적인 기준이라 셀 C 의 PASS 판정에는 영향 없음, ratio 2.30).
**해소**: 이 셀은 "정확도가 처리량보다 중요하고 처리량을 희생할 수 있는" 용처의 대안으로만
유효하다고 판단했다(carry-forward 아님 — 매 용처마다 재확정할 트레이드오프).

## 되풀이하지 말 것

1. Docker/torchvision/TP/tool_parser 벽은 자매 hint(B) 본문 §1·§3 참조 — **동일하게 적용된다**.
2. **"KV 를 fp8 로 양자화했으니 빠를 것"이라는 인상만으로 가중치 양자화를 생략하지 마라.**
   가중치(bf16 vs fp8)와 KV 캐시 양자화는 **서로 다른 축**이며, 단일스트림 속도는 가중치
   양자화가 지배한다(이 캠페인 실측: 가중치만 다르고 KV 는 같은 fp8 인데 26.83 vs 44.22
   t/s — 약 40% 저하). 처리량이 목표면 가중치도 양자화하라(자매 hint A/B 참조).
3. **루프라인(roofline.py)의 외부 레퍼런스(E)를 fp8/bf16 사다리 비교에 그대로 쓰지 마라** —
   현재 버전은 서빙시점 가중치양자화를 반영하지 않아 fp8 케이스의 물리상한을 과소평가한다
   (testlog §외부 레퍼런스(b) "후속 과제" 참조). 이 결함이 고쳐지기 전까지는 `--e-search
   empty` 로 결정론 루프라인만 정본으로 쓰는 편이 안전하다.
