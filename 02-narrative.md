# 2. 서사 — 빌드와 서빙이 어떻게 진행되었나

> 이 절은 **전부 Agent 저작**이다. 기계는 여기 한 글자도 쓰지 않는다(plan §6).
> **원문 전재 금지**(plan Q2) — devlog 실물에는 운영자 경로·호스트 지문이 있어
> 그대로 옮기면 배포 PII 표면이 커진다. **재현에 필요한 교훈만 추려 새로 쓴다.**

## 읽을 원재료 (복사 대상 아님 · 포인터)

- devlog: `../devlog/devlog_26090917_TP2_GPU2장_전부사용_광의탐색_캠페인_4셀.md`
- testlog: `../testlog/testlog_26090917_Qwen3.8.27B_TP2_광의탐색_4셀_FullBench_비교.md`

## 서사

이 태그는 TP=1 캠페인의 자매 hint `hint/0.28.0/qwen3.8-27b/rtxpro6000-main-native/
qfp8-len524288-kvfp8`(셀 D, YaRN 524288/factor=2.0)의 확장판이다 — **2배 예산(TP=2)으로
vLLM 공식 가이드가 예시로 든 정확한 상한(max-model-len=1,000,000, factor=4.0)까지 실측
도달**했다.

**증상(이 셀 고유, 캠페인 전체의 핵심 발견)**: 직전 TP=1 캠페인의 셀 D 는 "1M 컨텍스트는 이
GPU 1장(96GB)으로 물리적으로 불가"라고 잠정 결론했다(`docs/report/perf_26090916_qwen3.8-27b_
rtxpro6000_광의탐색_fullbench.md` §알려진 한계 참조). **원인**: 그 결론은 실측이 아니라
안전마진 추정이었다 — 실제로 1M 트라이얼을 시도하지 않고 예산 계산만으로 판단했다. **해소**:
TP=2(192GB combined) 로 실제 트라이얼을 돌려 batch=4 로 PASS 함을 확인했다 — 가중치가 카드당
절반이 되고 KV 도 TP 로 샤딩되는 효과가 겹쳐, 처음에 추정했던 것보다 훨씬 여유 있게 들어갔다
(required_kv=67.5GB < max_safe_kv, 정확한 여유분은
`docs/simlog/2026090908_1_qwen38_27b_cellH_tp2/run_summary.json` 참조).

**단일스트림 속도는 컨텍스트 상한과 거의 무관**: decode(69.57)는 같은 조합의 262144 자매셀
(E: 69.81)과 사실상 동일 — TP=1 캠페인(셀 D vs 셀 B)에서 이미 관찰된 패턴이 TP=2·1M 규모
에서도 재확인됐다(짧은 입출력 측정 조건에서는 decode 비용이 선언된 상한이 아니라 실제 KV
에 쌓인 토큰 수의 함수이기 때문 — 매우 긴 프롬프트/누적 컨텍스트에서는 다를 수 있다, 이
측정은 그 영역을 커버하지 않는다).

## 되풀이하지 말 것

1. Docker/torchvision/tool_parser 벽은 자매 hint(rtxpro6000x2 셀 E) §1·§3 참조.
2. **YaRN 확장 상한을 "물리적으로 불가"라고 예산 계산만으로 결론짓지 마라** — 이전 캠페인의
   그 결론은 실측 없이 안전마진으로 추정한 것이었고, 실제로는(다른 하드웨어 예산에서) 도달
   가능했다. 결론이 필요하면 실제로 트라이얼을 돌려라.
3. **YaRN rope_parameters 값을 추측하지 마라** — 모델의 실제 `config.json#text_config.
   rope_parameters` 를 직접 읽어 그대로 쓰고 `rope_type`·`factor` 만 목표 컨텍스트에 맞춰
   오버라이드했다(TP=1 캠페인 셀 D 와 동일 원칙, factor 값만 목표 비율에 맞춰 재계산).
