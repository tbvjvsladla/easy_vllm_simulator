# 2. 서사 — 빌드와 서빙이 어떻게 진행되었나

> 이 절은 **전부 Agent 저작**이다. 기계는 여기 한 글자도 쓰지 않는다(plan §6).
> **원문 전재 금지**(plan Q2) — devlog 실물에는 운영자 경로·호스트 지문이 있어
> 그대로 옮기면 배포 PII 표면이 커진다. **재현에 필요한 교훈만 추려 새로 쓴다.**

## 읽을 원재료 (복사 대상 아님 · 포인터)

- devlog: `../devlog/devlog_26090800_qwen3_4b_kv양자화_캠페인_resolve_빌드_셀A.md`
- testlog: `../testlog/testlog_26090818_plan26090813_단계6_재수행_종결판정.md`

## 서사

증상 → 원인 → 해소 순으로 이 셀이 무엇을 알려주는지 적는다.

**증상.** 24GB급 카드 예산에서 Qwen3-4B 를 띄우면 연산이 아니라 **용량**이 먼저 벽이 된다.
무양자화군은 8192+1024 토큰 워크로드에서 9번째 동시 요청부터 대기열이 섰다
(`bench_report_26090801` §2 — 상주 상한 A 8 · B 17 · C 25 · D 30).

**원인.** KV 풀이 11.0 GiB 절대 클램프로 고정돼 있고, 풀에 들어가는 토큰 수는 KV dtype 이 결정한다.
같은 바이트에서 엔진이 보고한 풀 토큰은 A 80,096 → B 160,192 → C 232,992 → D 277,920 이었다
(동 §1). 즉 압축비는 추정이 아니라 **엔진 보고값의 비**다.

**해소.** 이 셀은 `--kv-cache-dtype fp8` 하나로 풀을 2.00× 로 열었다(`160,192` 토큰).
대가는 고동시성 decode 다 — 동시성 1/2/4 에서 스트림당 20.20 / 22.38 / (L4 열사살 오염) tok/s
(동 §3 · judge PASS (explore · floor 10.1 · ratio 1.705)). 저동시성에서는 무양자화와 동급이고 FP8 은 오히려 상위였다.

**두 번 확인해야 할 것.** TurboQuant 는 첫·끝 2개 층을 FP16 으로 두는 설계라 압축비가 순 비트비보다
낮게 나오는 것이 **정상**이다(4bit 2.91× · 3bit 3.47× · 동 §1 이론 대비 열). FP8 만 이론 2× 와 정확히
일치했다. 이 비율은 층 구조에서 나오므로 **arch-invariant** 지만, 절대 tok/s 는 그렇지 않다(아래 §되풀이).

## 되풀이하지 말 것

- **절대 처리량을 실제 24GB 카드의 값으로 읽지 마라.** 이 측정은 KV 11.0 GiB 절대 클램프로
  *메모리 예산만* 모의했고 **아키텍처는 모의하지 않았다**(측정 HW 는 sm_121 통합메모리). 태그 arch 축이
  `sim-kv24g` 인 이유가 그것이다 — 옮겨도 되는 것은 **군 간 상대비**(arch-invariant)이고 절대 tok/s 는
  **arch-scaled** 다. 우리가 처음에 쓰려던 `rtx4090` 라벨을 버린 것도 같은 이유다.
- **GB10급 통합메모리 노드에서 in=8192 지속 포화부하를 동시성 4+ 로 길게 돌리지 마라.** SoC 열 보호가
  서빙을 종료시킨다(메인 2회 실측 · B 군 L4 는 그렇게 오염됐고 A 군 L8 도 절삭됐다 —
  `bench_report_26090801` §3 ⚠). 고동시성 비교가 필요하면 **냉각 인터벌을 설계에 넣어라**. 서브는 같은
  하중에서 피크가 낮았다 — 노드 간 하중 곡선이 다르므로 한쪽 실측으로 다른 쪽을 면제하지 마라.
- **`.env` 의 `IMAGE_TAG` 를 비우지 마라.** 비우면 compose 가 낡은 기본값으로 조용히 폴백해
  **다른 vLLM 버전을 측정**한다. 측정이 아니라 측정의 정체가 바뀌는 사고다.
- **prefix cache 를 켠 채 같은 프롬프트로 재면 그 수치는 무효다.** 요청마다 고유 프롬프트를 써
  hit 을 ≤2% 로 눌러야 한다(실측 0.1~0.35%).
- **TurboQuant 압축비를 순 비트비로 기대하지 마라.** 첫·끝 2층 FP16 면제가 설계다 — 3bit 가 4bit 대비
  1.19× 일 뿐 1.33× 가 아니다.
- **인증서를 찾지 마라 — 없는 것이 계약이다.** 목표를 세우지 않은 탐색(authority=explore)에서는 judge 가
  서술만 하고 인증서를 발행하지 않는다. 부재는 '성능이 나빴다' 가 아니라 '그 형태로 판정되지 않았다' 다.
